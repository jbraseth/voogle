# Copyright (c) 2025 Voogle Contributors
# Test and lint dependencies

# Core testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-env>=1.1.0
pytest-cov>=6.0.0

# Playwright for E2E/browser testing (run locally, not in CI)
# To run e2e tests locally: pip install playwright pytest-playwright && playwright install chromium
# playwright>=1.49.0
# pytest-playwright>=0.6.2

# Test utilities
pytest-xdist>=3.5.0  # Parallel test execution
pytest-timeout>=2.3.0  # Test timeouts
pytest-mock>=3.14.0  # Mocking utilities
httpx>=0.28.1

# Linting
ruff>=0.8.0
pyright>=1.1.390
